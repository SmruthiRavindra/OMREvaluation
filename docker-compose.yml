# =============================================================================
# docker-compose.yml
# Vision OMR System — Full-stack local development
#
# Usage:
#   docker compose up --build
#
# Services:
#   db       → PostgreSQL 16 (port 5432)
#   backend  → FastAPI / Python AI engine (port 8000)
#   gateway  → Express / Node.js data gateway (port 3000)
# =============================================================================

name: vision_omr_system

services:
  # ── PostgreSQL ─────────────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: omr_db
      POSTGRES_USER: omr_user
      POSTGRES_PASSWORD: omr_password_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U omr_user -d omr_db"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── FastAPI Backend AI ─────────────────────────────────────────────────────
  backend:
    image: vision_omr_system-backend:latest
    build: ./backend_ai
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./backend_ai/weights:/app/weights:ro
      - ./backend_ai:/app
    environment:
      - PYTHONUNBUFFERED=1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ── Node.js Data Gateway ───────────────────────────────────────────────────
  gateway:
    image: vision_omr_system-gateway:latest
    build: ./data_gateway
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      FASTAPI_URL: http://backend:8000
      PGHOST: db
      PGPORT: 5432
      PGDATABASE: omr_db
      PGUSER: omr_user
      PGPASSWORD: omr_password_dev
      PGSSL: "false"
    depends_on:
      db:
        condition: service_healthy
      backend:
        condition: service_healthy
    volumes:
      - ./backend_ai:/backend_ai:ro

  # ── CloudBeaver (DBeaver Web UI) ───────────────────────────────────────────
  cloudbeaver:
    image: dbeaver/cloudbeaver:latest
    container_name: vision_omr_system-cloudbeaver-1
    restart: unless-stopped
    ports:
      - "8978:8978"
    depends_on:
      db:
        condition: service_healthy


volumes:
  pgdata:
